# Byte Pair Encoding in OCaml

[Byte Pair Encoding](https://en.wikipedia.org/wiki/Byte-pair_encoding) is an algorithm that uses a subword tokenization technique to merge adjacent tokens together into new tokens.

  
